export function calculateSeverity({ impact='Low', affectedUsers=0, category='' } = {}) { const n=Number(affectedUsers)||0; return { severity:impact==='High'||n>=100?'Critical':impact==='Medium'||n>=10?'Medium':category==='Account compromise'?'High':'Low' }; }
export function generateReportDraft({ category='Not specified', severity='Not assessed', description='', timeline='' } = {}) { return `PRACTICE INCIDENT REPORT\\nCategory: ${category}\\nSeverity: ${severity}\\nWhat happened: ${description || 'Not provided'}\\nTimeline:\\n${timeline || 'Not provided'}\\nImmediate action: Stop interacting with the suspected threat, preserve useful evidence, and report through the approved channel.`; }
